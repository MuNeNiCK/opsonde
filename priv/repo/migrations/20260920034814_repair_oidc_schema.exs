defmodule Opsonde.Repo.Migrations.RepairOidcSchema do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:oidc_requests) do
      add :browser_binding_digest, :binary
    end

    alter table(:oidc_providers) do
      add :id_token_alg, :text, null: false, default: "RS256"
    end

    alter table(:user_identities) do
      modify :user_id, :uuid, null: false
      remove :refresh_token
      remove :access_token_expires_at
      remove :access_token
    end
  end

  def down do
    alter table(:user_identities) do
      add :access_token, :text
      add :access_token_expires_at, :utc_datetime_usec
      add :refresh_token, :text
      modify :user_id, :uuid, null: true
    end

    alter table(:oidc_providers) do
      remove :id_token_alg
    end

    alter table(:oidc_requests) do
      remove :browser_binding_digest
    end
  end
end
